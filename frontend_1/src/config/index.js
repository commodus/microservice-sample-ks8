//const dotenv = require("dotenv");

//const env = dotenv.config().parsed;

const config = {
  DataView_Frontend_Url_Base: process.env.DataView_Frontend_Url_Base,
  Web_Api_Url: process.env.Web_Api_Url,
};

export default config;
